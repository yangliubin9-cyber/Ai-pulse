// Optional runtime override. Copy to /config.js and reference from index.html
// at deploy time to point the SPA at a non-same-origin API without rebuilding.
window.__APP_CONFIG__ = {
  apiBaseUrl: '',
  appName: 'AI Pulse',
  version: '0.1.0',
};
